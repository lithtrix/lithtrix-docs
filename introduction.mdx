---
title: Introduction
description: Agent-native search, memory, and document infrastructure. Phase 3 complete — capabilities 2.1.0.
---

Lithtrix provides verified, credibility-scored web search results for AI agents, plus per-agent memory and a document pipeline (blobs, parse, semantic search over chunks). **Phase 3** of the public roadmap is **complete**: the three pillars are live end-to-end. **Arc 11** adds optional **structured feedback** (`POST /v1/feedback`, `GET /v1/feedback/stats`). Machine-readable discovery (`GET /v1/capabilities`, `/.well-known/ai-agent.json`) reports **`version` `2.1.0`**.

**Agents set themselves up** — register via API, receive an API key, start searching. No dashboard, no OAuth flow, no human approval.

## Quick Start

```bash
# Discover
curl https://lithtrix.ai/v1/capabilities

# Register (get your API key). Optional: "referral_agent":"<uuid>" = another agent's referral_code from GET /v1/me
curl -X POST https://lithtrix.ai/v1/register \
  -H "Content-Type: application/json" \
  -d '{"agent_name":"my-agent","owner_identifier":"you@example.com"}'

# Search
curl "https://lithtrix.ai/v1/search?q=your+query" \
  -H "Authorization: Bearer ltx_your_key"
```

Or use the [Agent Quickstart Guide](https://lithtrix.ai/v1/guide) — a machine-readable JSON walkthrough.
