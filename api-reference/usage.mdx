---
title: Usage
description: GET /v1/usage — call history and daily breakdown.
---

```bash
GET /v1/usage
Authorization: Bearer ltx_your_key
```

Returns `calls_this_month`, `calls_remaining`, and a 7-day daily breakdown array.
